@AGENTS.md

# 就活管理トラッカー

## ユーザーとの作業について

- 許可なくコードを編集しないこと。基本は学習者がコードを編集。直接編集するのは、その都度明示的に依頼された場合のみ。
- ユーザーは独学で、Yahoo Japan のエンジニア職への中途転職（未経験から中途）を目指しています。真面目な学生を教えるつもりで接し、近道を渡すのではなく、アーキテクチャ・保守性・可読性・セキュリティについて厳しく徹底的に指導してください。プロのエンジニアや面接官が求める水準を基準に、たとえ時間がかかっても、安易な道より厳密で意図的な道を優先してください。

## アプリの概要

就職・転職活動の情報（応募企業・選考ステータス・面接予定）を一元管理し、**面接の振り返りを蓄積して次の面接に活かす改善サイクルを回す**ためのアプリ。

- 「今の就活の全体像」と「次にやるべきこと」を1画面で見渡せることを価値とする。
- 開発者にとっての目的は、**Next.js と Tailwind CSS の新規習得**（最大の学習目的）、React / TypeScript の実践、およびポートフォリオの作成。
- 前作 `my-dedicated-hours`（1画面のSPA）からの発展として、**URLで画面が分かれる複数ページ構成**を扱うことが学習の中心。

## コマンド

| コマンド | 内容 |
| --- | --- |
| `npm run dev` | 開発サーバーの起動 |
| `npm run build` | 本番ビルド |
| `npm run start` | 本番ビルドの起動 |
| `npm run lint` | ESLint による検査 |
| `npm run typecheck` | TypeScript の型検査のみ(ビルドより速い) |

## ドキュメント

- `docs/learning-log.html` — 開発を通じて学んだことの記録。確定した理解に加え、**誤解していた内容とその訂正**を対で残す。区切りごとに追記・更新する。

## Git の規約

- **ブランチ名**は `種別/内容`（例：`chore/firebase-setup`）。**小文字・ケバブケース・英語**で統一する。大文字を使わないのは、macOS と Linux でファイルシステムの大文字小文字の扱いが異なり、事故の原因になるため。
- **コミットメッセージ**は `種別: 内容`（例：`chore: Firebase SDK を導入`）。内容は日本語でよいが、種別は英語で統一する。**絵文字（gitmoji）は使わない**——種別との二重表現になり、どれを選ぶかの判断コストが生まれるため。
- **種別**：`feat`（機能追加）/ `fix`（バグ修正）/ `chore`（設定・依存・ビルド）/ `docs`（ドキュメント）/ `refactor`（挙動を変えない内部改善）
- **1コミット＝1つの目的**。取り消せる単位がコミットであるため、複数の目的が混ざると `revert` や `bisect` が機能しなくなる。
- `main` に直接コミットせず、ブランチを切って作業する。ブランチの作成と移動は `git switch -c` を使う（`git branch -c` はブランチの複製であり、移動しない）。
- `git add .` は使わず、意図したファイルを明示的に指定する。コミット前に `git status` で `.env.local` が含まれていないことを必ず目視する。
- `package-lock.json` は必ずコミットする。コミットしないと、バージョン固定による環境間の再現性と、整合性ハッシュによる改竄検知の両方を失う。

## 技術スタック

- **Next.js (App Router)** — フレームワーク／開発サーバー／ビルド（新規習得）
- **React** — UIライブラリ
- **TypeScript** — 型安全（strict 設定を維持する）
- **Tailwind CSS** — スタイリング（新規習得。CSS Modules からの移行）
- **Firebase Authentication** — 認証（メール+パスワード / Google 認証）
- **Cloud Firestore** — データ永続化
- **Vercel** — ホスティング

採用しない技術：**Vite**（Next.js と役割が完全に重複する競合技術のため併用しない）、**独自バックエンド**（Firebase を BaaS として使い、フロントエンドの習得に集中する）。

## アーキテクチャの方針

- Firestore は `users/{uid}/` 配下で完結し、セキュリティルールで本人のみアクセス可能とする。
- 日付は `YYYY-MM-DD` 形式に統一する（前作の反省点。Firestore と state でフォーマットが分かれ、変換処理がバグの温床になった）。
- 認証状態は **Context + カスタムフック**（`useAuth()` 等）で共通化し、各ページに `onAuthStateChanged` の購読処理を重複させない。
- Firestore の呼び出しはデータ取得層（`lib/firestore.ts` 等）に集約し、ページに直書きしない。ページからは `getCompanies(uid)` のような意図が明確な関数を呼ぶだけにする。
- サーバーコンポーネントとクライアントコンポーネントの使い分けを意識する。
- 一覧ページには必ずエンプティステートを用意する（企業一覧・カレンダー・振り返り一覧・ダッシュボード）。
- 削除操作には必ず確認ダイアログを挟む。何が削除されるのか（企業名など）を明示し、破壊的操作と分かる配色（赤系）にする。
- 動的ルーティングは `[id]` で統一する（日付では対象を一意に特定できないため）。
- Firestore のセキュリティルールはコレクションごとに個別記述し、ワイルドカード（`{document=**}`）でまとめない。
- 入力フォームには必ずバリデーションを実装し、エラーは `alert` ではなく画面内に表示する。
- 編集はインライン編集ではなく専用ページ（`/[id]/edit`）へ遷移する方式に統一する（編集状態がURLで表現でき、リロードしても維持されるため）。
- ローディング／エラー表示は Next.js のファイル規約（`loading.tsx` / `error.tsx`）に沿って実装する。
- カレンダーUIは外部ライブラリを使わず自作する（学習目的が Next.js / Tailwind の習得であるため）。ただし日付計算に想定以上の時間がかかる場合は、その時点でライブラリ導入に切り替える判断も正しい。

## ルーティング設計

```
/                        ダッシュボード（直近の予定・サマリー・未対応アクション）

/companies               応募企業一覧
/companies/new           企業の新規登録
/companies/[id]          企業詳細
/companies/[id]/edit     企業の編集

/schedule                カレンダー（日付クリックでその日の予定一覧を展開）
/schedule/new            予定の新規登録
/schedule/[id]           予定詳細
/schedule/[id]/edit      予定の編集

/reviews                 面接振り返り一覧
/reviews/new             振り返りの新規登録
/reviews/[id]            振り返り詳細
/reviews/[id]/edit       振り返りの編集
```

各ディレクトリには、必要に応じて `loading.tsx` / `error.tsx` を配置する。

## データ設計（Firestore）

```
users/{uid}/
  ├─ companies/{companyId}
  │    ├─ name, position, appliedAt, status, jobUrl, memo
  │
  ├─ schedules/{scheduleId}
  │    ├─ date (YYYY-MM-DD), startTime, endTime, type, location
  │    ├─ companyId          ← companies への参照
  │    ├─ belongings: string[], preparation
  │
  └─ reviews/{reviewId}
       ├─ companyId          ← companies への参照
       ├─ scheduleId         ← schedules への参照（どの回の面接か）
       ├─ interviewDate (YYYY-MM-DD), questions: string[]
       ├─ wentWell, needsImprovement, nextAction
       └─ isActionCompleted: boolean
```

セキュリティルールは `firestore.rules` としてリポジトリ内でコード管理する。

## 将来の拡張候補（v1 では実装しない）

- **ダークモード** — 実装するなら、選考ステータスの色分け（青／黄／緑／灰）を暗い背景用にもう1セット設計する必要がある。**一部の画面だけ対応した中途半端な状態にはしない。**
- **AI機能** — 振り返りからの改善提案、想定質問の生成、傾向分析（仕様書9-1）。APIキーの扱いは下記「セキュリティ上の注意」に従う。
- **選考ステータスの変更履歴** — 現仕様はステータスの最新状態のみを保持する（仕様書9-2）。
- **Webフォントの導入** — 現在はシステムフォントに任せている。導入するなら `next/font` を使い、日本語フォントの容量とのトレードオフを検討した上で判断する。

## 環境変数

- Firebase の設定値は `.env.local`（gitignore 対象）で管理し、**Vercel 側にも同じ変数を登録する**。
- `.env.local` をコミットしない。

## セキュリティ上の注意

- 将来AI機能を追加する場合、**AIのAPIキーはクライアントに露出させず、必ずサーバー側（Route Handler）経由で使用すること**。Firebase の `apiKey` が公開前提なのとは性質が全く異なり、漏洩すると第三者による不正利用で直接的な金銭被害につながる。
- `NEXT_PUBLIC_` はセキュリティ機能ではなく**公開スイッチ**である。付けた値はビルド時にクライアントバンドルへ埋め込まれ、誰でも読める。付けてよいのは公開前提の値だけ。
- **依存パッケージは安易に増やさない。** `npm install` はファイルのダウンロードだけでなく、インストールスクリプト経由で**他人のコードを自分の権限で実行しうる**操作でもある。
- `npm warn allow-scripts` が出た場合は、該当スクリプトを実際に読んで必要性を判断してから許可・不許可を決める。不許可のまま `npm run build` / `npm run lint` を通し、問題が出ないことを確認する。
